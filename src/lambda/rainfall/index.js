const { resolvers } = require("./resolvers");
const { typeDef } = require("./typeDef");
const { RainfallStationsAPI } = require("./RainfallStationsAPI");

module.exports = {
  resolvers,
  typeDef,
  RainfallStationsAPI
};
